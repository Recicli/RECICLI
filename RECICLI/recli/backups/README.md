# Backups de EcoRed

Esta carpeta se usara unicamente para almacenar versiones antiguas o respaldos manuales del proyecto.

No debe usarse como fuente principal del sistema. El codigo oficial de EcoRed vive solamente en `/pages`, `/css`, `/js`, `/data` y `/assets`.
